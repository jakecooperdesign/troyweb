{% if menu %}
	<ul class="navbar-nav">
	{% for item in items %}
		<li class="{{ item.classes | join(' ') }} nav-item">
			<a target="{{ item.target }}" href="{{ item.link }}" class="nav-link">{{ item.title }}</a>
		</li>
	{% endfor %}
	</ul>
{% endif %}
